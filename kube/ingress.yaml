apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: server
            port: 
              number: 8002
      - path: /
        pathType: Prefix
        backend:
          service:
            name: client-service
            port: 
              number: 80